# SOURCECODESYSTEM
Built with PHP (native, no framework) and MySQL for the database, managed through phpMyAdmin. Uses PHPMailer with SMTP for OTP email verification, PHP sessions for secure user and admin authentication, a live chat feature, GCash payment integration, an admin dashboard, file upload/download management, and an HTML/CSS/JavaScript frontend.
